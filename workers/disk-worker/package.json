{
	"name": "webvm-disk-worker",
	"version": "0.1.0",
	"private": true,
	"type": "module",
	"scripts": {
		"prepare:disk": "node scripts/prepare-disk.mjs",
		"dev": "wrangler dev",
		"deploy": "wrangler deploy",
		"typecheck": "tsc --noEmit"
	},
	"devDependencies": {
		"@cloudflare/workers-types": "latest",
		"typescript": "latest",
		"wrangler": "latest"
	}
}
